---
name: bolu-yidong
description: |
  波动率异动预警。把一份「预警信号单」播报给你：观测站（数据从哪来）、观测目标
  （哪个合约）、预警等级（平静 ● / 正常 ●● / 活跃 ●●● / 剧烈 ●●●●，剧烈档附
  「⚠ 波动率爆发」标记）、观测数据（近 7 天年化波动率、近 30 天基准、放大倍数、
  ATR、逐日波动曲线）、预警依据（是哪条线触发的，具体到数值）与防灾提示。
  它不预测价格涨跌方向，只回答一件事：市场的「运动速度」是不是突然变快了。
  判定引擎为本仓库自研（sources.py 的 calculate，纯函数：各通道把 K 线直接喂给
  引擎原函数，引擎一行未改），结果可复现、不依赖任何大模型。
  数据来自两条路：与网页版同源的多平台公开行情（Hyperliquid → OKX → Bybit 三连），
  以及币安官方公开资源——合约公开 K 线接口（fapi.binance.com）、官方开源数据仓库
  （data.binance.vision 1h K 线月度 + 日度归档）、官方 binance 技能驱动的官方 CLI
  （binance-cli）。
  当用户问「现在 BTC 波动大不大」「市场是不是突然变疯 / 太安静」「波动率是不是
  爆了」「该不该收着点杠杆」时使用。
metadata:
  version: 1.0.0
  author: dddwww12e2r1r
  category: market-analysis
  engine: sources.py 的 calculate（纯函数直接喂数据，命令行与网页共用同一段代码，引擎未改）
  official_resources:
    - name: fapi.binance.com
      role: 币安合约公开接口（免密钥）。--live 通道取 1h K 线 1000 根（自动选路：直连不通自动探测本机代理端口）
    - name: data.binance.vision
      role: 币安官方开源数据仓库（T+1 归档 ZIP）。--official 通道用 futures/um/monthly/klines 月度 + futures/um/daily/klines 日度的 1h K 线拼到 ≥720 根，播报截至前一交易日的信号单
    - name: binance-cli
      repo: https://github.com/binance/binance-cli
      role: 官方命令行工具。--skill 通道由官方 binance 技能文档记载的 request 子命令直取合约公开 K 线
  optional_skills:
    - name: binance
      reason: 本技能通过官方 binance 技能文档记载的 binance-cli request 用法取数
  license: MIT
---

# 波动率异动预警 · 官方技能说明

> 这个技能是一间气象观测站：你报一个合约交易对，它播报一张**预警信号单**——
> 观测站、观测目标、预警等级、观测数据、预警依据、防灾提示。
> 所以这份说明书也按气象台的规矩写：先说它播报什么、不播报什么，
> 再说信号单怎么播出来的、预警等级怎么定的，最后是「读信号单时别犯的错」。

---

## 这个观测站播报什么

- 「现在 BTC 波动大不大？」→ 预警等级 + 近 7 天年化波动率（平静 ● / 正常 ●● / 活跃 ●●● / 剧烈 ●●●●）
- 「市场是不是突然变疯了？」→ 预警依据：近 7 天波动率相对近 30 天基准放大了多少倍，到没到预警线
- 「最近是不是太安静了？」→ 平静档：年化波动率低于 35%，市场运动缓慢
- 「该不该收着点杠杆？」→ 剧烈档触发「⚠ 波动率爆发」时，防灾提示会建议重检杠杆、仓位与止损设置
- 「这个等级是怎么定出来的？」→ 见下方《预警发布规范》，公式完全公开

## 这个观测站不播报什么

- **不播报方向**。波动率说的是「运动速度」，不是「往哪边运动」——剧烈档不等于要看跌，也不等于要看涨
- **不播报点位**。没有任何「目标价 / 支撑位 / 压力位」，也永远不该有
- **不给下单建议**。信号单里没有「该买该卖」的字眼，只有风险提示
- **不看资金费率 / 多空比 / 链上数据**。本站只观测价格波动本身，
  那些属于另外的作品
- **不构成投资建议**。播报用于观察与研究

---

## 四条观测通道（四条数据通道）

同一个引擎（`calculate` 纯函数），四条通道，信号单抬头都会写明本次用的是哪条：

| 通道 | 怎么触发 | 数据从哪来 | 口径 | 通道标识 |
|---|---|---|---|---|
| 多平台行情（默认） | `python agent.py BTCUSDT` | 多平台公开行情：Hyperliquid → OKX → Bybit 三连（与网页版同源同引擎） | 实时 | `source=realtime via=market-route` |
| 合约实时 | `python agent.py BTCUSDT --live` | 币安合约公开接口 1h K 线 1000 根：直连不通自动探测本机代理端口 | 实时 | `source=live via=fapi-route` |
| 官方 CLI | `python agent.py BTCUSDT --skill` | 官方 binance 技能驱动的官方 CLI（`binance-cli request GET`）直取合约公开 K 线 | 实时 | `source=skill via=binance-cli` |
| 官方归档 | `python agent.py BTCUSDT --official` | 官方开源数据仓库：1h K 线月度归档 + 日度归档拼到 ≥720 根 | **T+1**：截至上一个已发布归档日的收盘口径，不是此刻实时值 | `source=official via=official-archive` |

气象台规矩，写在代码里而不是写在文档里：

- **观测做不了就明说，不拿演示数据充数**。命令行取不到数就如实报错退出
  （网页版保留演示降级，并在页面上写明「演示数据」）。
- **默认通道与网页数值完全一致**：同一批 K 线喂给同一个 `calculate` 函数。
- **CLI 没装就如实回退**。`--skill` 通道找不到官方 CLI 时，回退到合约接口自动选路，
  信号单里写明真实路径，JSON 里多出 `requestedSource: "skill"` 字段——它宁可说没调成，也不假装调过。
- **各大平台口径差异如实披露**：合约实时通道与多平台通道的行情来自不同平台，
  波动率数值会有小幅不同（趋势一致），信号单备注里写明。

---

## 预警信号单每个项目是什么

| 信号单项目 | 意思 | 数据口径 |
|---|---|---|
| 观测站 | 本次数据从哪来 | 四条通道之一，附实际来源（如 Hyperliquid perpetual live / 直连 / 归档锚定日期） |
| 观测目标 | 观测哪个合约 | USDT 本位永续合约交易对，如 BTCUSDT |
| 预警等级 | 四档之一，点数越多越剧烈 | 平静 ● / 正常 ●● / 活跃 ●●● / 剧烈 ●●●●；剧烈档附「⚠ 波动率爆发」 |
| 观测数据·近7天年化波动率 | 近 168 根 1h K 线对数收益率的年化标准差 | 引擎 `hv7`；括号里附近 30 天（`hv30`）参考值 |
| 观测数据·相对基准放大 | 近 7 天波动率 ÷ 基准 | 引擎 `ratio`；基准 = 近 30 天去掉最近 24 小时的年化波动率 |
| 观测数据·ATR（14小时） | 近 14 根 1h K 线平均真实波幅占价格的百分比 | 引擎 `atr`，观察短期波幅的另一把尺子 |
| 观测数据·逐日波动曲线 | 最近 7 个「24 小时窗口」各自的单日年化波动率 | 引擎 `curve`，用 `→` 串起来看变化趋势 |
| 预警依据 | 具体是哪条线触发的 | 如实到数值：如「放大 1.92 倍（预警线 1.8 倍）」，两条线都超就都列 |
| 防灾提示 | 怎么正确理解这次播报 | 方向免责 + 杠杆止损提醒 + 通道备注 + 免责声明 |

`--json` 输出同样的内容，另带 `generatedAt`、`archive`（归档锚定日期、用了哪些
月份与日期、K 线根数、缺哪些）、`requestedSource`（如实回退标注）、
`honesty`（完整防灾提示清单）等字段，便于程序化调用。

---

## 预警发布规范（引擎公式，全部公开）

**1. 波动率怎么算**（`calculate`）：

- 对数收益率：`ln(收盘价[i] / 收盘价[i-1])`
- 年化：`标准差 × √(24×365) × 100`（1 小时 K 线，一年 24×365 根）

**2. 四档怎么划**：

- **剧烈**（●●●●，⚠ 波动率爆发）：放大倍数 ≥ 1.8 倍，**或**近 7 天年化 ≥ 120%
- **活跃**（●●●）：放大倍数 ≥ 1.3 倍，**或**近 7 天年化 ≥ 70%
- **正常**（●●）：近 7 天年化 ≥ 35%
- **平静**（●）：其余

**3. 基准怎么取**：近 30 天（720 根）年化波动率，**去掉最近 24 小时**——
防止「正在爆发的 7 天」把基准一起抬高压低放大倍数（否则爆发会被基准掩盖）。

**4. 两条预警线的关系**：放大倍数线（1.8 / 1.3）与绝对年化线（120% / 70%）是
「或」的关系——任一条触发就升档，信号单会写明具体是哪条（或两条）触发的。

---

## 防灾提示（读信号单别犯的错）

- **波动率不是方向**：剧烈档只说明市场运动速度加快，多空都可能——
  把它当「系好安全带」的提醒，不是「掉头」的信号。
- **`--official` 是 T+1 口径**：信号单抬头写明锚定日期；归档拼出的 K 线根数
  不足 720 根时会在备注里写明「30 天基准按现有样本计算，口径略有放宽」。
- **缺月缺天不补数**：当月归档月末才发布，缺的月份与日期在备注里如实列出。
- **不同平台数值略有差异**：多平台 / 合约实时 / 官方归档的行情来自不同平台，
  同一时刻的波动率数值会有小幅不同（趋势一致），防灾提示里写明本次来源。
- **命令行不做演示数据降级**：取不到数就如实报错退出。
- **研判仅供观察与研究，不构成投资建议。**
