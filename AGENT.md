# AGENT.md · 波动率异动预警 · 值班记录

> 一句话：这是网页版的**命令行分身** —— 信号单可以播四条通道（含币安官方三路），
> 判定引擎是同一段代码。这份文档按气象台值班室的方式写：**先看两份真实的信号单，
> 再教你三条核查命令，最后是值班纪律。**

---

## 先看结论：一份真实的信号单

下面是 `python agent.py BTCUSDT` 在 **2026-09-14 实测**的原样输出
（行情在动，你跑出来的数字一定不同；但版式、字段、通道标识应当一致）：

```text
波动率异动预警 · 通道：market（默认，与网页同源）

  ──────────────────────────────────────────────────────────
   波动率异动预警 · 预警信号单    2026-09-14 21:57 UTC
  ──────────────────────────────────────────────────────────
   观测站    多平台公开行情（Hyperliquid → OKX → Bybit 三连，与网页同源同引擎） · 本次实际来源：Hyperliquid perpetual live
   观测目标  BTCUSDT
   预警等级  ● 平静
  ──────────────────────────────────────────────────────────
   观测数据
   · 近7天年化波动率      32.54%   （近30天 41.57%）
   · 相对基准放大          0.78 倍  （基准 41.88%）
   · ATR（14小时）        0.606%
   · 逐日波动曲线      30.31 → 36.55 → 30.99 → 55.14 → 7.11 → 16.25 → 31.95
   预警依据
   · 近7天年化波动率 32.54%（低于正常线 35%）
   防灾提示（别过度解读）
   · 预警说的是「运动速度」不是方向：波动率升高不等于要看跌，也不等于要看涨
   · 波动率爆发时段杠杆和止损更容易被异常波动扫到：重检杠杆、仓位与止损设置
   · 与网页版同源同引擎：同一批K线喂给同一个 calculate 函数，数值完全一致
   · 45 秒缓存只存在于网页版；命令行每次现取
   · 研判仅供观察与研究，不构成投资建议
```

信号单每一部分是谁产出的：

| 信号单部分 | 谁负责 |
|---|---|
| 抬头（观测站 / 观测目标 / 预警等级） | 数据通道如实自报 + **自研引擎**判定 |
| 观测数据（hv7 / hv30 / 放大倍数 / ATR / 曲线） | **自研引擎**（`sources.py` 的 `calculate`，与网页版同一段代码）原样输出 |
| 预警依据（具体哪条线触发） | 同上，引擎结果 + 触发线如实换算 |
| 防灾提示 | 程序自己声明，不靠文档补 |

再看官方归档通道的信号单（同日实测，`python agent.py ETHUSDT --official`），
重点在防灾提示区多出来的几条诚实声明：

```text
   观测站    各大平台官方开源数据仓库（1h K线月度 + 日度归档，T+1）（截至 2026-09-13）
   观测目标  ETHUSDT
   预警等级  ●● 正常
  ──────────────────────────────────────────────────────────
   观测数据
   · 近7天年化波动率      54.82%   （近30天 58.08%）
   · 相对基准放大          0.94 倍  （基准 58.59%）
   · ATR（14小时）        0.590%
   · 逐日波动曲线      33.8 → 37.72 → 45.6 → 48.34 → 112.91 → 16.45 → 39.42
   预警依据
   · 近7天年化波动率 54.82%（达到正常线 35%，未到活跃线 70%）
   防灾提示（别过度解读）
   · 归档是 T+1：本信号单截至 2026-09-13 收盘口径（1h 整点K线），不是此刻实时值
   · 归档拼出 1056 根 1h K线，满足 30 天基准口径
   · 研判仅供观察与研究，不构成投资建议
```

---

## 教你核查：三条，每步一条命令

不要求你相信上面任何一句描述。三条命令，看的是程序**自己写的通道标识**——
它写的就是它真走的，不会替你圆场。

### 核查 1 · 看它自报家门

```bash
python agent.py BTCUSDT --json
```

看 JSON 开头的三个字段（信号单抬头里也有同样内容）：

```json
"source": "realtime",
"via": "market-route",
"dataSource": "多平台公开行情（Hyperliquid → OKX → Bybit 三连，与网页同源同引擎） · 本次实际来源：..."
```

`via`（实际走的路）有四个值：`market-route` = 多平台行情（默认通道）、
`fapi-route` = 合约公开接口自动选路（`--live`）、`binance-cli` = 官方 CLI、
`official-archive` = 官方开源数据仓库。

### 核查 2 · 看官方 CLI 是不是真的被调用

```bash
python agent.py BTCUSDT --skill --json
```

**CLI 通了 `via` 应当变成 `binance-cli`。** 如果你自己的机器 CLI 没装、
或它连不上接口，程序会**如实回退**到合约接口自动选路（2026-09-14 本机实测即此形态）：

```json
"source": "skill",
"via": "fapi-route",
"requestedSource": "skill"
```

`requestedSource: "skill"` 就是回退声明：本次请求的是官方 CLI 通道，
实际走了自动选路，信号单抬头与防灾提示里都会写明。—— 它宁可说自己没调成，也不假装调过。

### 核查 3 · 看官方归档的日期锚得对不对

```bash
python agent.py BTCUSDT --official --json
```

看 `archive` 字段（2026-09-14 实测样例）：

```json
"archive": {
  "anchor": "2026-09-13",
  "daysUsed": ["2026-09-13", "2026-09-12", "..."],
  "monthsUsed": ["2026-08"],
  "candles": 1056
}
```

官方开源数据仓库是 **T+1** 归档：今天的文件还没发布，`anchor` 一定是
「最近一个已发布归档日」（程序从昨天往前逐日探测，命中即用）。
拼出的 K 线根数在 `candles` 字段如实展示（≥720 根才算满足 30 天基准口径，
不足时防灾提示会写明「按现有样本计算，口径略有放宽」），缺的月份日期进
`missingDays` / `missingMonths` 如实列出，**不补齐、不猜数**。

---

## 四条通道怎么选

| 怎么播 | 数据从哪来 | 适合什么时候 |
|---|---|---|
| `python agent.py BTCUSDT` | 多平台公开行情三连（与网页版同源同引擎） | 日常查询，与网页版互相印证 |
| `python agent.py BTCUSDT --live` | 币安合约公开接口 1h K 线 1000 根（自动选路） | 想用币安合约口径观测时 |
| `python agent.py BTCUSDT --skill` | 官方技能 binance + 官方 CLI | 要体现官方技能调用链时 |
| `python agent.py BTCUSDT --official` | 官方开源数据仓库（K 线月度 + 日度归档） | 免代理环境、或要 T+1 历史口径时 |

自然语言也认：`python agent.py "用官方归档看看 ETHUSDT"` 会自动选
`--official` 通道；识别不出通道词就不吭声走默认，识别不出币种会注明
「按默认 BTCUSDT 观测」——**绝不拿一句话乱猜**。

不带参数或 `python agent.py web` 启动本地网页服务（默认端口 8002，
`PORT` 环境变量优先——与改造前部署方式完全一致）。
常用参数：`--json`（纯 JSON，进度与信号单走 stderr）、`--host` / `--port`（网页模式）。
全部用法 `python agent.py --help`。

---

## 官方资源 ↔ 通道对应行

| 币安官方资源 | 用在哪一步 | 信号单里对应哪一行 | 实测证据 |
|---|---|---|---|
| 合约公开接口 **fapi.binance.com** | `--live` / `--skill` 两条通道的数据源：`/fapi/v1/klines` 1h K 线 1000 根 | 抬头「观测站」整行 | 实测第 1 轮选路即成功出单 |
| 官方技能 **binance** + 官方 CLI | `--skill`：`binance-cli request GET` 直取上述接口 | 抬头「via=binance-cli」（或回退时的 `requestedSource=skill`） | 实测回退形态如实标注，JSON 与防灾提示双写明 |
| 官方开源数据仓库 **data.binance.vision** | `--official`：`futures/um/monthly/klines` 月度 + `futures/um/daily/klines` 日度，拼到 ≥720 根 | 抬头「截至 2026-09-13」+ 防灾提示区根数与缺档声明 | 实测锚定 2026-09-13，拼出 1056 根 |

**价值边界一句话**：本作品的默认通道刻意与网页版同源（多平台公开行情），
币安官方三路是另外三条可选通道——四条通道喂的都是同一个引擎，
比对不同数据源的波动率读数本身就是一种交叉验证；
「波动率四档 + 爆发预警」这个判定产物是自研——官方给的是原始 K 线，
没有「把 7 天 / 30 天波动率、放大倍数、ATR 拼成一张带分档预警的信号单」
这个东西，这是本作品的价值。

**引擎没被动过的证据**：`sources.py` 由改造前的网页版 `agent.py` **逐字节改名**而来
（SHA-256 校验一致，备份在 `_backup/`）。`calculate` 是纯函数——四条通道把各自
拿到的 K 线**直接喂给引擎原函数**，不复制、不改写任何判定逻辑；
网页版继续走 `analyze()` 同一入口。命令行不做演示数据降级：
默认通道刻意绕开会降级的 `analyze()`，取不到数就如实报错退出，
**绝不展示演示数据冒充信号单**。

---

## 官方 20 个技能的取舍（逐个核过，不是看了看文档）

币安官方技能市场共 20 个技能，分两组。本作品只接了 1 个，理由如下：

| 技能 | 结论 |
|---|---|
| `binance` | ✅ **在用** —— `--skill` 通道经它驱动的官方 CLI 取数 |
| `academy-skill` / `fiat` / `square-post` | ❌ 接口走 `www.binance.com`，实测连接被拒绝（502） |
| `p2p` | ❌ 走 `c2c.binance.com`，不可达 |
| `payment` / `onchain-pay` | ❌ 必须自备 API 密钥并签名；本作品是纯公开数据作品，不用密钥 |
| Web3 组 13 个 | ⚠️ 主力接口全部挂在 `web3.binance.com`，实测被 SNI 层阻断；其中 `query-token-info` 的 kline 子命令可达，但它与已接入的官方合约 K 线来源（fapi / 官方归档）重复，未接入 |

另外，官方各语言 SDK 连接器（connector 类仓库）也没有引入：它们面向
「用 API 密钥交易账户」的场景，本作品只读公开数据，引入会变成死依赖。

> 关于「官方是不是已有波动率类能力」：官方接口给的是**原始 K 线**；
> Web3 组确有信号类技能但接口不可达，且输出的是官方模型的综合判定。
> 本作品是**可逐项复核的规则口径**（年化公式、四档阈值、两条预警线的
> 「或」关系全部公开在 SKILL.md，输入是什么、每一步怎么算的全部可算）。互补，不重复。

---

## 值班纪律（读信号单前先读这个）

1. **波动率不是方向**：剧烈档（⚠ 波动率爆发）只说明市场运动速度加快，
   多空都可能——把它当「系好安全带」的提醒，不是「掉头」的信号。
2. **`--official` 是 T+1 口径**：信号单抬头写明锚定日期；K 线根数不足 720 根时
   防灾提示写明「30 天基准按现有样本计算，口径略有放宽」。
3. **缺月缺天不补数**：当月归档月末才发布，缺的如实列出。
4. **不同平台数值略有差异**：多平台 / 合约实时 / 官方归档的行情来自不同平台，
   同一时刻的波动率读数会有小幅不同（趋势一致），信号单写明本次来源。
5. **命令行不做演示数据降级**：取不到数就如实报错退出（网页版有演示降级并在页面声明）。
6. **CLI 没装就如实回退**：`requestedSource: "skill"` 写明真实路径，不假装调过。
7. **研判仅供观察与研究，不构成投资建议。**

---

## 与已提交参赛说明的关系

- **网页形态**（`static/index.html` + `sources.py`）继续保留，功能与口径都不变，
  部署方式不变（`python agent.py` 或 `PORT` 环境变量，默认 8002）；
- **改造内容**：原网页版 `agent.py` 逐字节改名为 `sources.py`（引擎模块），
  新增命令行入口 `agent.py`（四通道 + 自然语言 + 预警信号单）与
  技能定义 `skills/bolu-yidong/SKILL.md`，让这个作品可以被技能安装器装载、
  被其他 Agent 直接调用；
- 两种形态**共用同一段判定引擎**（`sources.py` 的 `calculate`），
  结论口径完全一致。
