# 波动率异动预警智能体｜项目说明

## 一、项目名称

- 中文名称：波动率异动预警智能体
- 仓库前缀：`bolu-yidong-agent`
- 项目目录：`D:\币安作品6`

## 二、项目简介

波动率异动预警智能体是一款面向海外用户的加密货币合约市场风险观察工具。它不预测价格涨跌方向，而是监控市场运动速度是否突然加快，帮助用户识别潜在的风险升温阶段。

用户输入一个 USDT 永续合约，例如 `BTCUSDT`、`ETHUSDT` 或 `SOLUSDT`，系统会读取小时级 K 线，计算历史波动率和 ATR，并将当前市场状态划分为平静、正常、活跃、剧烈四档。当近期波动率相对历史基准明显放大时，系统会标记“波动率爆发”。

Agent 化改造后，本项目拥有两种形态：**网页监控面板**（原功能不变）与**命令行智能体**（`agent.py`，四条数据通道 + 自然语言入口 + 预警信号单输出），两种形态共用同一段判定引擎（`sources.py` 的 `calculate`，由改造前网页版主程序逐字节改名而来，引擎一行未改）。

## 三、核心功能

### 1. 历史波动率监控

- 获取指定合约最近约 30 天的 1 小时 K 线
- 使用小时级对数收益率计算年化历史波动率
- 展示近 7 天的波动率轨迹
- 同时提供近 30 天波动率参考值

### 2. 波动率突变预警

系统将近 7 天波动率与历史基准进行比较：

- 当前波动率放大达到基准的 1.8 倍，触发“波动率爆发”
- 或当前年化波动率达到 120%，触发“波动率爆发”
- 触发后页面提示用户重新检查杠杆、仓位和止损设置

### 3. ATR 风险参考

系统计算最近 14 小时的 ATR，并以价格百分比展示，用于观察近期平均真实波幅变化。

### 4. 波动率分档

- 平静：年化波动率低于 35%
- 正常：年化波动率达到 35%
- 活跃：年化波动率达到 70%，或相对基准放大达到 1.3 倍
- 剧烈：年化波动率达到 120%，或相对基准放大达到 1.8 倍

## 四、数据来源与可靠性设计

项目不调用 Binance 接口。当前数据源优先级如下：

1. Hyperliquid Perpetual 公共 K 线接口
2. OKX Swap 公共 K 线接口
3. Bybit Linear 公共 K 线接口
4. 所有实时接口均不可用时，切换到明确标注的演示数据

Hyperliquid 使用公开的 `candleSnapshot` 接口，不需要 API Key，也不涉及自动交易或账户授权。

系统在服务端读取行情，避免浏览器直接访问第三方接口时遇到跨域问题。接口返回空响应、无效 JSON、HTTP 错误或网络超时时，后端会统一处理，前端不会再因为空响应出现 `Unexpected end of JSON input`。

### Agent 化改造后的命令行数据通道

命令行智能体（`agent.py`）在网页同源的多平台行情之外，新增三条币安官方公开资源通道：

1. 默认通道（与网页同源）：多平台公开行情三连（Hyperliquid → OKX → Bybit），同一批 K 线喂给同一个 `calculate` 函数，数值与网页完全一致
2. `--live`：币安合约公开接口 1h K 线 1000 根（直连不通自动探测本机代理端口）
3. `--skill`：币安官方 binance 技能驱动的官方 CLI（`binance-cli request GET`）直取，CLI 不可用时如实回退到自动选路并在 JSON 里标注 `requestedSource`
4. `--official`：币安官方开源数据仓库（data.binance.vision 的 1h K 线月度 + 日度归档），拼到 ≥720 根后喂给引擎，T+1 口径、免代理

命令行**不做演示数据降级**：取不到数就如实报错退出（网页版保留演示降级并明确标注）。

## 五、项目工作流程

```text
用户输入合约
      ↓
后端校验 USDT 永续合约格式
      ↓
请求 Hyperliquid 1小时 K线
      ↓
失败时依次尝试 OKX / Bybit
      ↓
计算对数收益率、ATR、历史波动率
      ↓
对比近30天基准均值
      ↓
判断波动率等级和是否爆发
      ↓
网页展示指标、曲线和风险提示
```

## 六、技术实现

### 后端

- Python 3.10+
- `http.server.ThreadingHTTPServer`
- Python 标准库 HTTP 客户端
- 内存缓存，默认缓存时间约 45 秒
- 支持读取部署环境中的 `PORT` 变量
- 监听地址为 `0.0.0.0`，适合公网部署

### 前端

- HTML + CSS + JavaScript
- 深色风险监控界面
- 无需构建工具
- 支持通过后端服务直接访问
- 前端采用安全文本解析方式处理 API 响应

## 七、本地运行

在命令行中进入项目目录后运行：

```bash
python agent.py --port 8002
```

然后打开：

```text
http://127.0.0.1:8002
```

不能直接双击 `static/index.html` 使用，因为 `file://` 页面没有后端服务，无法访问 `/api/analyze` 接口。

## 八、在线版本

在线访问地址：

[波动率异动预警智能体](https://3a92e18687384a4bb5dc4587978299ef.app.workbuddy.link)

公网版本已经验证可以返回：

- HTTP 200
- Hyperliquid perpetual live
- `demo=false`
- BTCUSDT 实时波动率指标

## 九、GitHub 上传文件

建议上传以下文件：

```text
bolu-yidong-agent/
├── agent.py              # 命令行智能体入口（四通道 + 自然语言 + 网页服务）
├── sources.py            # 判定引擎模块（改造前网页版主程序逐字节改名，含网页 Handler）
├── README.md
├── AGENT.md              # 值班记录：真实信号单样张、核查命令、技能取舍
├── 项目说明.md
├── .gitignore
├── static/
│   └── index.html
└── skills/
    └── bolu-yidong/
        └── SKILL.md      # 官方技能定义（预警发布规范）
```

不需要上传 `__pycache__`、`.pyc`、`_backup` 与本地测试脚本。

## 十、Agent / 智能体说明

本项目中的智能体流程不是简单展示一组固定数据，而是按照以下步骤自动完成分析：

1. 接收用户输入的合约代码
2. 校验输入是否为 USDT 永续合约格式
3. 选择可用的行情接口（网页：多平台三连降级；命令行：四条通道任选）
4. 获取并整理小时级 K 线
5. 自动计算 ATR、7 天历史波动率和 30 天基准
6. 判断波动率是否处于异常放大状态
7. 生成波动率等级、预警状态和风险解释
8. 网页形态：返回到监控面板；命令行形态：播报预警信号单或输出纯 JSON

命令行智能体的判定引擎与网页是同一段代码：`sources.py` 由改造前的网页版
主程序逐字节改名而来（SHA-256 校验一致），各通道把拿到的 K 线直接喂给
引擎原函数 `calculate`——不复制、不改写任何判定逻辑，两种形态结论口径完全一致。

## 十一、使用限制

- 项目仅用于市场观察、风险教育和参赛展示
- 不构成投资建议、交易建议或收益承诺
- 不执行下单，不连接用户交易账户
- 实时接口受到第三方服务可用性和网络环境影响
- 当实时数据不可用时，页面会明确显示演示数据，不会伪装成实时行情

## 十二、命令行智能体使用指南

### 12.1 四条命令

```bash
python agent.py BTCUSDT                  # 预警信号单：多平台公开行情（与网页同源同引擎）
python agent.py BTCUSDT --live           # 币安合约公开K线（自动选路）
python agent.py BTCUSDT --skill          # 币安官方 CLI（binance-cli request）直取
python agent.py BTCUSDT --official       # 币安官方开源数据仓库归档（T+1，免代理）
```

不带参数或 `python agent.py web` 启动网页服务（默认 8002 端口，`PORT` 环境变量优先，
与改造前部署方式完全一致）。

### 12.2 自然语言入口

一句话里认通道词和币种名，识别不出通道词就走默认，识别不出币种会注明
「按默认 BTCUSDT 观测」——绝不拿一句话乱猜：

```bash
python agent.py "用官方归档看看 ETHUSDT"     # 自动选 --official 通道
python agent.py "用官方技能查一下 SOL"       # 自动选 --skill 通道
python agent.py "实时看看 BTC 波动大不大"    # 自动选 --live 通道
```

### 12.3 参数表

| 参数 | 作用 |
|---|---|
| `--live` | 币安合约公开接口通道（自动选路：直连不通自动探测本机代理端口） |
| `--skill` | 官方 CLI 通道（CLI 不可用时如实回退并标注 `requestedSource`） |
| `--official` | 官方开源数据仓库通道（T+1，免代理） |
| `--json` | stdout 只输出纯 JSON，进度与信号单走 stderr（供程序调用） |
| `--port N` / `--host` | 网页模式监听端口（`PORT` 环境变量优先）与地址 |
| `--help` | 全部用法 |

### 12.4 通道标识表

| 通道 | source | via | 口径 |
|---|---|---|---|
| 默认（多平台行情） | `realtime` | `market-route` | 实时，与网页同源同引擎 |
| `--live` | `live` | `fapi-route` | 实时（币安合约 1h K 线） |
| `--skill` | `skill` | `binance-cli`（或回退时 `fapi-route` + `requestedSource=skill`） | 实时 |
| `--official` | `official` | `official-archive` | T+1（截至上一归档日收盘） |

### 12.5 三步核查（每步一条命令）

```bash
python agent.py BTCUSDT --json            # 看 source / via / dataSource 自报家门
python agent.py BTCUSDT --skill --json    # 看 CLI 回退形态：requestedSource=skill
python agent.py BTCUSDT --official --json # 看 archive 锚定日期与 K 线根数
```

它写的就是它真走的：通道标识由程序自己写入，回退与缺档如实标注，
不补齐、不猜数、不拿演示数据充数。
